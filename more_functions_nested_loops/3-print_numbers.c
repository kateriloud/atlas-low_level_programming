#include <stdio.h>

/**
 *
 * print_numbers - function the numbers 0 to 9 followed by a new line
 *
 * @n: 0 to 9
 *
 * Return: (0)
 *
 */
void print_numbers(void)
{
	int n;

	if (n = 48; n < 58; n++) 
	{
		putchar(n);
	}
	putchar('\n');
}
