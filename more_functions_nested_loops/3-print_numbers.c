#include <stdio.h>

/**
 *
 * print_numbers - function the numbers 0 to 9 followed by a new line
 *
 * @n: 0 to 9
 *
 * Return: (0)
 *
 */
void print_numbers(void)
{
	int n;

	if (n = 0; n > 9; n++) 
	{
		putchar(n);
	}
	putchar('\n');
}
