#include <ctype.h>
#include "main.h"
#include <stdio.h>

int _isupper(int c)
{
	char c;

	if (c = 65; c <= 90; c++)
	{
		_putchar(c);
		return (1);
	}

	if (c = 97; c <= 122; c++)
	{
		_putchar(c);
		return(0);
	}
}
