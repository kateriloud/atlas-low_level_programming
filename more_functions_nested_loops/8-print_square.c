#include <stdio.h>
#include "main.h"

/**
 * print_square - prints a square
 * return: (0)
 *
 */
void print_square(int size)

int x;
int y;
{
for (x = 1; x <= 4; x++)
	{
		for (y = 1; y <= 4; y++)
		{
			putchar('#');
			putchar('\n')
		}
		if ( x <=0)
		{
		putchar('\n');
		}
	}
	
}
