#include <stdio.h>
#include "main.h"

/**
 * print_diagonal - will print diagonal line
 * @n: diagonal line
 *
 * return: (0)
 *
 */
void print_diagonal(int n)
{
int x;
	if (x <= 0)
	{
		putchar('\n')
	}
	else
	{
		for (x = 0; x < n; x++)
		{
		putchar(92);
		}
		{
		putchar('\n');
		}
	}
}
