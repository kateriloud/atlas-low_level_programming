#include <stdio.h>
#include <stdlib.h>

/**
 *
 * main - Prints all lowercsae letters a-z except q and e
 *
 * Return: (0)
 *
 */
int main(void)
{
	char i;

	while (i = 'a'; i <= 'z'; i++);
		{
		if (i != 'e' && 'q')
		{
		putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
