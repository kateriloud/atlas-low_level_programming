#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints all possible cominations of single digit numbers
 *
 *  Return: (0)
 *
 */

int main(void)

{
	int k;

	for (k = 48 ; k < 57; k++)
	{
		putchar(' ');
		putchar(',');
	}
	if (k = 57)
		putchar(k);
	
	putchar('\n');
	return (0);

}
