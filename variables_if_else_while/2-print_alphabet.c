#include <stdio.h>

/**
 * main - This prints all lowercase letters 'a-z'
 *
 * Return: (0) 
 *
 *
 **/
int main()
{
	int ch;
	for(ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	for(ch)
		putchar(\n);
	return 0;
}
