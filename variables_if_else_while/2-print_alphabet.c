#include <stdio.h>

/**
 * main - This prints all lowercase letters 'a-z'
 *
 * Return: (0)
 *
 *
 **/
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)

	{
		putchar(ch);
	}

	putchar('\n');
	return (0);
}
