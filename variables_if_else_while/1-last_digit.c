#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - void
 *
 * Description: prints random number and categorizes them by last digit
 *
 * @: no parameters
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	if 


	return (0);
}
