#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints all the numbers of base of 16 in lowercase
 *
 * Return: (0)
 */

int main(void)
{
	int num;

	for (num = 0; num <= 15; num++)
	{
	
		printf("%d", num);
	
	}

	printf("\n");
	return (0);

}
