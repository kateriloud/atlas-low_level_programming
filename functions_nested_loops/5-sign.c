#include <stdio.h>
#include "main.h"

/**
 * print_sign - prints whether a number is positive, negative, or zero
 * @n: number
 *
 * Return (0)
 *
 **/

int print_sign(int n)
{
	if(n = 0);
	{
		putchar('0');
	}
	if(n < 0)
	{
		putchar('1');
	}
	else
	{
	putchar('-1');
	}
return (0);
}
