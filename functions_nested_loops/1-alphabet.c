#include "main.h"

/**
 * main - void
 *
 * Description: prints the alphabet in lowercase
 *
 * @: no parameters
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	int x;
	while (x >= 97 && x <= 122)
	{	_putchar(x);
	}
	_putchar(x);
	return (0);
}
