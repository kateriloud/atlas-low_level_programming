#include <stdio.h>
#include "main.h"
/**
 * _abs(int) - computes absolute value of integer
 *
 * return: (0)
 *
 */
int abs(int  x)
{
	if (x > 1)
		return (x);
	else
	{
		x = -1;
		return (x);
	}
return (0);
}
