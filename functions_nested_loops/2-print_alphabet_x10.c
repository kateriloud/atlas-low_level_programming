#include "main.h"
#include <stdio.h>
/**
 * print_alphabet_x10 - print a to z ten times
 *
 *Return: (0) always
 **/
void print_alphabet_x10(void)
	
{
int ch;

	for (ch = 'a'; ch <= 'z'; ch++);
	{
		_putchar(ch);
		_putchar('\n');
	}
return (0);
}
