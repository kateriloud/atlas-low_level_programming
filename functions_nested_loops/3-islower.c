#include <stdio.h>
#include <stdlib.h>
#include <main.c>

/**
 *
 * main - function that checks for lowercase characters
 * @c - character to check
 * 
 * Return: (0)
 * */

int islower(int c)
{
	
	if (c >= 'a'; && c <= 'z')
	{
		return 1;
	}
		else
		{
			return 0;
		}


return (0);
}
