#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * main - entry point
 * Description: program that searches and prints 1 for upper/lowercase
 * Return: Always 0 (Success)
 */

int _isalpha(int c)
{
	
	{
		_putchar(ch);
	}
	else
		{
		_putchar(1);
		}

	return 0;
}
