#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include "main.h"

/**
 * add - entry point
 * Description: function that adds two ints and returns result
 * @x, y: int
 * Return: Always 0 (success)
 */

int add(int x, int y)

{
	return (x + y);
}
