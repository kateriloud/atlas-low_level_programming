#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include "main.h"

/**
 * add - entry point
 * Description: function that adds two ints and returns result
 * @: int
 * Return: Always 0 (success)
 */

int add(int, int)
{
	int x,y;
	
	result = x + y

	return(result)
}
