#include "main.h"
#include <string.h>
/**
 * _strlen - returns length of a string
 * @s - string integer
 * return: (0)
 *
 */
int _strlen(char *s)
{
int k;

k = '0';

	while ([k] != \0)
	{
		k++;
	}
return (0);
}
