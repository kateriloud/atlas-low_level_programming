#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps values of two integers
 * @a: integer
 * @b: integer
 * return: (0)
 *
 */
void swap_int(int *a, int *b)
{
	int x = *a;
	*a = *b;
	*b = x
}
